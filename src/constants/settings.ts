// Constants and settings for the application
export const SETTINGS = {
  // Header height in pixels
  HEADER_HEIGHT: 60,
  // Full AppShell footer area (background music + footer links)
  FOOTER_HEIGHT: 40,
  // Background music strip fixed height
  BACKGROUND_MUSIC_HEIGHT: 85,
  // Additional padding for elements that need to avoid overlapping the header
  HEADER_PADDING: 20,
} as const;
